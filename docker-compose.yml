version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - db-data:/app/data
      - ./private-key.pem:/app/private-key.pem:ro
    restart: unless-stopped

volumes:
  db-data:
